import { useRef, useState } from "react"


const App = () => {
  const [socket , setSocket] = useState();
  const inpurRef = useRef();
  return (
    if(1soxket)
    <div>
      
    </div>
  )
}

export default App

